org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
systemProp.file.encoding=UTF-8

# FIX: Disable incremental compilation to allow cross-drive (C: to D:) bridging
kotlin.incremental=false
kotlin.incremental.java=false
kotlin.incremental.js=false

android.useAndroidX=true
android.enableJetifier=true
android.ignoreAarMetadataCheck=true

# Opt out of AGP 9+ new DSL interface — our templates target AGP 8.1.1.
# Required when Flutter SDK >= 3.27 is used on the build machine, since the
# updated flutter_tools gradle plugin detects AGP 9+ transitive deps
# (e.g. camera_android_camerax) and switches to new-DSL-only mode, which
# causes a NullPointerException in the project evaluation listener.
android.newDsl=false
